## AssetLister

We're going to create a website

It should list all packaged ship hulls of one character in Dal 6. In eve online.

This needs an esi-key.

As a first step, it should be static, but updated by a python script, running and modifying the git repository.

It should be hosted on github static pages.

